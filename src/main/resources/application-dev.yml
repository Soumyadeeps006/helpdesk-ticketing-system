spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

logging:
  level:
    com.helpdesk: DEBUG
    org.springframework.amqp: DEBUG
    org.hibernate.SQL: DEBUG